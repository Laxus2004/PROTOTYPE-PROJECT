# PROTOTYPE-PROJECT
All in 1

// this is for hashing the password so it would be hidden if access the databases
$password = md5($_POST['password']);
---- below normal password are shown ----
$password = $_POST['password'];
------------------------------------------------------------------------------------------------------------------
